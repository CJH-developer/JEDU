<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.geomin.project.UserServiceTwo.UserServiceTwoMapper">
  <select id="MyList" resultType="GameContentVO">
	select * from game_content as e inner join game_purchase as d on
	e.user_no = d.user_no inner join study_group as f on e.user_no =
	f.user_no order by e.game_no  desc limit #{JTwoPageList}, #{JTwoAmount}; 
  
  
  
  
  
  
  
  
  
  
  </select>
    <select id="MyToTal" resultType="int">
    select count(*) as total from game_content as e inner join game_purchase as d on e.game_no = d.game_no inner join study_group as f on e.game_no = f.game_no; 
  </select>
  
  </mapper>